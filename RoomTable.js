import { useEffect, useState } from 'react'
export default function RoomTable({roomId}){ const [cards,setCards]=useState([]); function deal(){ setCards(['Aâ™ ','Kâ™¥']) } return (<div className='bg-white/5 p-6 rounded'><div className='mb-4 flex justify-between items-center'><h3 className='text-lg'>ط·ط§ظˆظ„ط© ({roomId})</h3><div><button onClick={deal} className='px-4 py-2 bg-emerald-500 rounded'>ط³ط­ط¨ ط£ظˆط±ط§ظ‚</button></div></div><div className='relative'><img src='/assets/table_ruby.png' className='w-full h-64 object-cover rounded shadow' alt='table'/></div><div className='mt-4 flex gap-3'>{cards.map((c,i)=>(<div key={i} className='w-20 h-28 bg-white/10 rounded flex items-center justify-center text-lg'>{c}</div>))}</div></div>) }
